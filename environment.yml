# conda env create environment.yml
# source activate BackupHub
# source deactivate
name: BackupHub
channels:
  - defaults
dependencies:
  - python=3.5
  - pip:
    - python-gitlab==0.13
    - PyGithub==1.26.0
